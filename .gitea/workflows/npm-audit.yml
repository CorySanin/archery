name: NPM Audit Check

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 16 * * 5'
  
jobs:

  npm_audit:
    name: Check NPM audit
    runs-on: ubuntu-latest-little
    timeout-minutes: 20
    strategy:
      fail-fast: true
    permissions:
      contents: read

    steps:
    - name: Checkout repository
      uses: https://github.com/actions/checkout@v4

    - name: set up pnpm
      uses: pnpm/action-setup@v5

    - name: NPM Audit
      run: pnpm audit
